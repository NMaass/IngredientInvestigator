import React from "react";
import Camera from "./Camera";

const Launch = ({setLaunch}) => {
    return(
        <div>
            <Camera setLaunch={setLaunch}/>
        </div>
    )
}
export default Launch;